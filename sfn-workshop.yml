name: sfn-workshop
channels:
  - conda-forge
  - defaults
dependencies:
  - jupyter
  - matplotlib > 3.0
  - numpy > 1.17
  - pandas > 0.25
  - scikit-image > 0.16
  - scikit-learn > 0.21
  - scipy > 1.3
